import React from "react";
import '../styles/Header.scss';

const Header = () => {
  return (
    <header class="header">
      <div class="conainer"></div>
    </header>
  );
};

export default Header;
